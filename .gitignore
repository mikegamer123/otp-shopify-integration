# Secrets — never commit. Merchant keys and Shopify tokens live here.
.env
.env.*
!.env.example

node_modules/
npm-debug.log*

# Local payment audit log written by lib/audit.js (dev only)
data/
*.log
